'use client';

type EventFormProps = {
  userId: string;
  type: 'create' | 'update';
};

const EventForm = ({ userId, type }: EventFormProps) => {
  return (
    <div>
      
    </div>
  )
};

export default EventForm;
